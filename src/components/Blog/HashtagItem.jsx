function HashtagItem({ title }) {
  return (
    <div className="px-3 py-1 rounded-sm cursor-default dark:bg-blue-400 dark:text-gray-900 text-blue-500 bg-sky-100 sm:text-sm text-xs">
      {title}
    </div>
  );
}

export default HashtagItem;
